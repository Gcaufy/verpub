{
  "name": "sempub-js-test",
  "version": "0.0.1",
  "scripts": {
    "test:bin": "NODE_ENV=testing ../../../bin/sempub publish -q --dry-run",
    "test": "NODE_ENV=testing npm run test:bin && find . | grep test.js | xargs mocha"
  }
}
